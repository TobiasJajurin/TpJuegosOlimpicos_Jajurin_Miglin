<div style="height: 8rem;"></div>
<head>
  <main class="cardC">
        <div class="cardCreditos">
            <div class="card" style="width: 18rem; float:  left; ">
                <img src="~/imgs/PityMartinez.jpg" class="card-img-top" alt="...">
                <div class="card-body">
                  <h5 class="card-title">Tobias Jajurin</h5>
                  <p class="card-text">Hincha de river como toda persona del bien.</p>
                </div>
              </div>
        </div>
        
        <div class="cardCreditos">
          <div class="card" style="width: 18rem; float:  left;">
            <img src="~/imgs/JuanferQuintero.jpg" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">Ian Miglin</h5>
              <p class="card-text">Hincha de river como toda persona del bien.</p>
            </div>
          </div>
        </div>
          <div class="cardCreditos">
          <div class="card" style="width: 18rem; float:  left;">
            <img src="~/imgs/gallardo.jfif" class="card-img-top" alt="...">
            <div class="card-body">
              <h5 class="card-title">Marcelo Gallardo</h5>
              <p class="card-text">El mejor entrenador de la historia del futbol argentino.</p>
            </div>
          </div>
        </div>
        <style>
        #ball {
            width: 50px;
            height: 50px;

            border-radius: 50%;
            position: absolute;
            bottom: 0;
        }
    </style>
</head>
<body>
    <div id="ball"><img src="~/imgs/Anillos.png"></div>

   <script>
        const ball = document.getElementById('ball');
        let x = 0; 
        let y = 0; 
        let dx = 2; 
        let dy = 2; 
        const ballSize = 50; 

        function animate() {
            const ballRect = ball.getBoundingClientRect();
            const windowWidth = window.innerWidth;
            const windowHeight = window.innerHeight;

            
            x += dx;
            y += dy;

            
            if (x + ballSize > windowWidth || x < 0) {
                dx = -dx; 
            }

            
            if (y + ballSize > windowHeight || y < 0) {
                dy = -dy; 
            }

            
            ball.style.left = `${x}px`;
            ball.style.top = `${y}px`;

            
            requestAnimationFrame(animate);
        }

        
        animate();
    </script>
   
</body>
</main>